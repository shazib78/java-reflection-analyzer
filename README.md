# Java-Reflection-Analyzer

Java Reflection Analyzer is a Java-based library for analyzing Java reflection in the top 200 popular Maven projects using static analysis. It utilizes SootUp to detect reflection calls, identify factors influencing them, and statically predict runtime values for these invocations.
